require("dotenv").config();
const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "src", "config", "database", "index.js"),
  "migrations-path": path.resolve(
    __dirname,
    "src",
    "config",
    "database",
    "migrations"
  )
};
